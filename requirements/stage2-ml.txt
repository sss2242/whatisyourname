# Stage 2: Machine learning and statistics (medium install, ~1-2min)
# Depends on stage1-core.txt being installed first.
scikit-learn==1.8.0
statsmodels==0.14.6
xgboost==3.2.0
ruptures==1.1.10
hmmlearn==0.3.3
arch==8.0.0
shap==0.50.0
mapie==1.3.0
dtaidistance==2.4.0
PyWavelets>=1.7
ripser==0.6.14              # Persistent homology (replaces giotto-tda which needs scikit-learn==1.3.2)
ta>=0.11                    # Technical analysis indicators (ADX, OBV, BB, MACD)
lifelines>=0.29             # Cox PH survival analysis
changefinder>=0.3           # Online change point detection (SDAR)
copulae>=0.7                # Copula fitting (Gaussian, Student-t, Clayton) with AIC selection
stumpy>=1.13                # Matrix Profile motifs and discords
miceforest>=5.7             # LightGBM-based MICE imputation
SALib>=1.5                  # Sobol global sensitivity analysis
scikit-fuzzy>=0.5           # Mamdani fuzzy inference engine
tigramite>=5.2              # PCMCI causal discovery
optuna>=4.0                 # TPE hyperparameter optimization
statsforecast>=2.0          # Fast AutoARIMA (100x faster than pmdarima)
vaderSentiment>=3.3         # Rule-based sentiment analysis (VADER)
EMD-signal>=1.6             # CEEMDAN cycle decomposition
tempdisagg>=0.2.13          # Chow-Lin/Denton temporal disaggregation (frequency separator)
