# Operator 1 -- Python dependencies
# Core
requests>=2.28
pandas>=1.5
numpy>=1.23
pyarrow>=10.0
pyyaml>=6.0
python-dotenv>=1.0  # .env file support for local development

# Temporal modeling -- Phase 6
# T6.1: regime detection + causality
statsmodels>=0.14
scikit-learn>=1.2
ruptures>=1.1
hmmlearn>=0.3

# T6.2: forecasting models (actively used in forecasting.py)
arch>=6.0
xgboost>=1.7
torch>=2.0

# T6.1: Bayesian change-point detection (used in regime_detector.py)
pymc>=5.0

# Phase F: Advanced modules
shap>=0.42             # SHAP explainability (F2)
mapie>=0.8             # Conformal prediction alternative (F1)

# Phase G: DTW and uncertainty
dtaidistance>=2.3      # Dynamic Time Warping for historical analogs (G1)

# Not yet implemented -- uncomment when adding these modules:
# SALib>=1.4          # Sobol sensitivity analysis
# TA-Lib>=0.4         # Candlestick pattern detection
# deap>=1.4           # Genetic algorithm optimisation
# pywt>=1.4           # Wavelet decomposition

# Report generation (Phase 7, used in report_generator.py)
matplotlib>=3.6

# News sentiment (Step 5e, used in news_sentiment.py)
gnews>=0.4.0               # Google News scraper (no API key) -- primary news source
feedparser>=6.0            # RSS feed parser (Google News RSS fallback, no key)

# Not yet used:
# plotly>=5.13

# Phase 1: Unofficial wrappers for core markets
edgartools>=3.0             # US SEC EDGAR (primary) -- code handles both old cash_flow() and new cashflow_statement() APIs
sec-edgar-api>=1.0         # US SEC EDGAR (fallback)
jquants-api-client>=2.0.0  # Japan J-Quants (primary) -- replaces edinet-tools. Free plan via email registration at jpx-jquants.com
dart-fss>=0.4              # South Korea DART (primary)
ixbrl-parse>=0.10.0           # UK iXBRL document parser for Companies House financial data
# pyesef -- REMOVED: library archived 2026-01-03 (graceful degradation in eu_esef_wrapper.py)
# pycvm -- optional: file-based API, br_cvm_wrapper.py uses direct HTTP as primary path
pycvm>=0.4                 # Brazil CVM data (optional, used for local file parsing)

# Phase 2: Tier 2 market wrappers (optional -- graceful degradation if not installed)
# akshare>=1.18             # China SSE/SZSE (comprehensive: stocks, financials, macro) -- research: .roo/research/cn-sse-2026-02-24.md
# jugaad-data>=0.29         # India NSE/BSE (stock data, RBI rates) -- research: .roo/research/in-bse-2026-02-24.md
# ixbrl-parse -- already listed above in Phase 1 section

# Phase 2b: OHLCV per-region wrappers (primary sources, graceful fallback to yfinance)
yfinance>=1.2.0            # Global OHLCV fallback (no key) -- research: .roo/research/ohlcv-yfinance-2026-02-24.md
pykrx>=1.2.4               # Korea OHLCV primary (no key) -- research: .roo/research/ohlcv-per-region-2026-02-25.md
twstock>=1.4.0             # Taiwan OHLCV primary (no key) -- research: .roo/research/ohlcv-per-region-2026-02-25.md
# akshare>=1.18             # China OHLCV primary (no key, heavy deps) -- optional, uncomment if needed
jugaad-data>=0.29          # India OHLCV primary (no key) -- research: .roo/research/ohlcv-per-region-2026-02-25.md

# Phase 3: Macro data libraries (per-region primary + wbgapi fallback)
wbgapi>=1.0.13             # Global macro fallback (no key) -- research: .roo/research/macro-wbgapi-2026-02-24.md
fredapi>=0.5.2             # US FRED macro primary (needs FRED_API_KEY) -- research: .roo/research/macro-per-region-2026-02-25.md
sdmx1>=2.25                # EU/ECB/Bundesbank macro primary (no key) -- research: .roo/research/macro-per-region-2026-02-25.md
python-bcb>=0.3.3          # Brazil BCB macro primary (no key) -- research: .roo/research/macro-per-region-2026-02-25.md
banxicoapi>=1.0.2          # Mexico Banxico macro primary (needs BANXICO_TOKEN) -- research: .roo/research/macro-per-region-2026-02-25.md

# Testing
pytest>=7.0
