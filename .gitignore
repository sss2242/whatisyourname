**/__pycache__/
.pytest_cache/
*.pyc

# Secrets (never commit)
.env

# Cache artifacts (cache/.gitkeep is tracked to preserve the directory)
cache/
!cache/.gitkeep

# Prevent accidental commit of git internals as a regular folder
git/
